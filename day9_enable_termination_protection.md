EC2 termination protection is a safety feature provided by AWS that prevents an Amazon EC2 instance from being accidentally deleted (terminated) via the AWS Management Console, API, or CLI.

With the instance selected, click Actions at the top. \
Go to Instance settings → Change termination protection. \
Check the box for Enable (or select Enable). \
